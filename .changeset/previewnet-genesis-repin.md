---
"@parity/truapi": patch
"@parity/truapi-provider": patch
---

The `truapi-host` CLI previewnet preset and truapi-provider's previewnet catalog carry the relay, Asset Hub, Bulletin and People genesis hashes previewnet reports after its latest reset, and the bundled previewnet chain specs match them.
