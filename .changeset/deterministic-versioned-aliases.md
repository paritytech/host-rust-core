---
"@parity/truapi": minor
---

The unprefixed name for a versioned type belongs to the newest version any wrapper selects. `HostLocalStorageReadError`
is the v02 shape; the v01 one is `V01HostLocalStorageReadError`.
