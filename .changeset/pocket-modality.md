---
"@parity/truapi": minor
"@parity/truapi-host": minor
---

Add the `pocket` service: `listSubscribe` over the calling product's cards, and `removeCard`. A host serves them by
supplying the optional `pocket` callbacks.
