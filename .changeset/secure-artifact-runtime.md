---
"@parity/truapi-host": minor
---

Bind product runtimes, durable allowances, and automatic signing grants to the trusted executable artifact digest, and authenticate local CLI product-frame connections with per-process capabilities.
